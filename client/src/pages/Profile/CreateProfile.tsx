import React from 'react'
import { IProps } from 'types'

const CreateProfile : React.FC<IProps> = () =>{
    
    return (
        <section className='CreateProfile'>
            <h1>Create Profile</h1>
        </section>
    )
}

export default CreateProfile